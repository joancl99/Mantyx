# Add files here to ignore them from prettier formatting
/dist
/coverage
# Lockfile is machine-generated; pnpm rewrites it on install and prettier must not touch it
pnpm-lock.yaml
/.nx/cache
/.nx/workspace-data
.nx/self-healing
.angular
# Hand-formatted per-client feature flags — keep the readable one-field-per-line
# layout so editing/removing a module is fast; prettier would collapse it.
apps/web/public/app-config.json
